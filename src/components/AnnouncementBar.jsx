import React from 'react';

export default function AnnouncementBar() {
  return (
    <div className="announcement-bar">
      <span className="announcement-text">Announcement: Spring ASB Election is now live for voting!</span>
      <a href="#survey" className="survey-link">Survey here</a>
    </div>
  );
}